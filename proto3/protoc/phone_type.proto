syntax = "proto3";

package PHONE_TYPE;

enum PhoneType
{
    UNKNOWN = 0;
    MOBILE = 1;
    HOME = 2;
    WORK = 3;
}

message Person
{
    string name = 1;
    int32 id = 2;
    string email = 3;
    PhoneType phone_type = 4;
    // also you can have repeated PhoneType
}